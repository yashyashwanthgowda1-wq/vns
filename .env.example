NEXT_PUBLIC_ENQUIRY_API_URL=http://localhost:4000/api/enquiries
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api

# Sanity project settings (public identifiers, not secrets).
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
# Optional server-only Viewer token for a private dataset. Never use NEXT_PUBLIC_ for tokens.
SANITY_API_READ_TOKEN=
